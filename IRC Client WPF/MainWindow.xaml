<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:System="clr-namespace:System;assembly=mscorlib" x:Class="IRC_Client_WPF.MainWindow"
        Title="MainWindow" Height="490.652" Width="724.37" Closing="Window_Closing">
    <Grid>
        <TreeView x:Name="UIServerList" HorizontalAlignment="Left" Margin="10,10,0,35" Width="118
                  "/>
        <RichTextBox x:Name="UIChatBox" Margin="133,10,133,35" IsReadOnly="True">
            <RichTextBox.Resources>
                <Style TargetType="{x:Type Paragraph}">
                    <Setter Property="Margin" Value="0"/>
                </Style>
            </RichTextBox.Resources>
            <FlowDocument/>
        </RichTextBox>
        <ListView x:Name="UINickList" Margin="0,10,10,35" HorizontalAlignment="Right" Width="118">
            <System:String>+abcdefghijklmnop</System:String>
        </ListView>
        <RichTextBox x:Name="UIInputBox" Margin="10,0,10,10" KeyUp="InputBox_KeyUp" Height="20" VerticalAlignment="Bottom">
            <FlowDocument>
                <Paragraph>
                    <Run Text=""/>
                </Paragraph>
            </FlowDocument>
        </RichTextBox>
    </Grid>
</Window>
